# AI_WP_Dynamic_Cache_Plugin
 A WordPress “signed edge agent” cache plugin + Cloudflare Workers control plane that dynamically optimizes caching across Cloudflare Edge, R2, origin disk, and object cache, using on-demand sandbox benchmarks per VPS to select the fastest safe strategy. Part of the AI_WP_Plugin_Family
